zero-trust/tpm/*.ctx
zero-trust/tpm/*.pem
zero-trust/tpm/*.pub
zero-trust/tpm/*.priv
zero-trust/tpm/*.bin
zero-trust/tpm/*.sig
zero-trust/tpm/*.out
zero-trust/tpm/*.msg
zero-trust/tpm/*.pcrs
zero-trust/tpm/*.hash
**/*.pyc
**/__pycache__/
zero-trust/agents/*
zero-trust/logs/*

# Compiled binaries
# TPM binaries (exclude .c source files)
zero-trust/spire-tpm-plugin/tpm-app-*
zero-trust/spire-tpm-plugin/ma-app-*
!zero-trust/spire-tpm-plugin/*.c
zero-trust/spire-tpm-plugin/*.o
zero-trust/spire-tpm-plugin/*.ctx
zero-trust/spire-tpm-plugin/*.pem
zero-trust/spire-tpm-plugin/*.pub
zero-trust/spire-tpm-plugin/*.priv
zero-trust/spire-tpm-plugin/*.bin
zero-trust/spire-tpm-plugin/*.sig
zero-trust/spire-tpm-plugin/*.out
zero-trust/spire-tpm-plugin/*.msg
zero-trust/spire-tpm-plugin/*.pcrs
zero-trust/spire-tpm-plugin/*.hash

# SPIRE binaries
**/bin/spire-server
**/bin/spire-agent
**/bin/oidc-discovery-provider

# Rust Keylime binaries
**/target/release/keylime_agent
**/target/release/keylime_ima_emulator
**/target/release/keylime_push_model_agent
# Rust build artifacts (target directory)
**/target/

# WASM binaries (compiled, not source)
**/target/**/*.wasm
**/wasm-plugin/target/**/*.wasm
# But allow WASM source files
!**/*.wat
!**/src/**/*.rs

# Go binaries (hostproximity, etc.)
**/hostproximity
**/*.exe

# SPIRE binaries in various locations
# Note: **/spire-server and **/spire-agent are intentionally NOT here —
# those patterns match source directories (e.g. spire-fork/cmd/spire-server/).
# The specific paths below cover the actual built binaries.
**/cmd/spire-server/spire-server
**/cmd/spire-agent/spire-agent
**/support/oidc-discovery-provider/oidc-discovery-provider

# Shared libraries and dynamic libraries
**/*.so
**/*.dll
**/*.dylib

# Rust build artifacts (dependency files, etc.)
**/*.d
**/.cargo-lock

# hybrid-cloud-poc/code-rollout-phase-1/spire/spire-agent
# hybrid-cloud-poc/code-rollout-phase-1/spire/spire-server

# Generated private keys and certificates (hybrid-cloud-poc)
# Private keys (always ignore for security)
hybrid-cloud-poc/**/*-private.pem
hybrid-cloud-poc/**/private.yml
hybrid-cloud-poc/**/*.key
# Generated TLS/CA directories
hybrid-cloud-poc/**/cv_ca/
hybrid-cloud-poc/**/*_ca/
hybrid-cloud-poc/**/tls/
# Generated certificate files
hybrid-cloud-poc/**/*-cert.crt
hybrid-cloud-poc/**/cacert.crt
hybrid-cloud-poc/**/ca-public.pem
hybrid-cloud-poc/**/server-cert.crt
hybrid-cloud-poc/**/client-cert.crt
hybrid-cloud-poc/**/cacrl.der
hybrid-cloud-poc/**/cacrl.pem
# Generated database files
hybrid-cloud-poc/keylime/cv_data.sqlite
# But allow test data certificates (test fixtures)
!hybrid-cloud-poc/**/testdata/*.pem
!hybrid-cloud-poc/**/testdata/*.crt
!hybrid-cloud-poc/**/testdata/*.key
!hybrid-cloud-poc/**/test/**/testdata/*.pem
!hybrid-cloud-poc/**/test/**/testdata/*.crt
!hybrid-cloud-poc/**/test/**/testdata/*.key

# SPIRE Development Environment (temporary, created by spire-dev-setup.sh)
build/spire-dev/
spire-overlay/.backup-*/

# SPIRE overlay build workspace (intermediate artifacts from spire-build.sh)
# Use spire-fork/ and spire-fork-sdk/ for the committed, editable source tree.
build/spire/
build/spire-api-sdk/
build/spire-binaries/
build/BUILD_INFO.txt
.spire-version

# Environment files
**/.env
**/.env.local
**/.env.*.local
**/.envrc
!**/.env.example
